import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <div className="weather_navbar">
        <header className="weather_header">
          <h1 className="weather_heading">Weather Forecast</h1>
        </header>
      </div>
    );
  }
}

export default Header;
